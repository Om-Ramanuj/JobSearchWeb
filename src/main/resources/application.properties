# === App Name ===
spring.application.name=JOB

# === MongoDB Configuration ===
spring.data.mongodb.uri=mongodb+srv://root:root@cluster0.ksefquq.mongodb.net/Om?retryWrites=true&w=majority&appName=Om
spring.data.mongodb.database=Om

# === Server Port (default to 8080 if PORT not set) ===
server.port=${PORT:8080}

# === Logging Configuration ===

# Show DEBUG for Spring Data MongoDB (shows repository queries)
logging.level.org.springframework.data.mongodb=DEBUG

# Reduce MongoDB Java Driver noise to INFO
logging.level.org.mongodb.driver=INFO

# Optional general Spring Web logging (for troubleshooting web requests)
# logging.level.org.springframework.web=INFO

# Optional Hibernate/JPA logs (if applicable)
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql=TRACE
